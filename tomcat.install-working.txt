sudo dnf update -y
sudo dnf install java-17-amazon-corretto-devel -y # Or the desired Java v                                                                                        ersion
cd /opt
sudo wget https://archive.apache.org/dist/tomcat/tomcat-9/v9.0.87/bin/apa                                                                                        che-tomcat-9.0.87.tar.gz # Replace with the desired Tomcat version
sudo tar -xvf apache-tomcat-9.0.87.tar.gz
sudo mv apache-tomcat-9.0.87 tomcat
sudo nano /opt/tomcat/conf/tomcat-users.xml
sudo groupadd tomcat
sudo useradd -s /bin/nologin -g tomcat -d /opt/tomcat tomcat
sudo chown -R tomcat:tomcat /opt/tomcat
sudo chmod +x /opt/tomcat/bin/*.sh
sudo nano /etc/systemd/system/tomcat.service
[Unit]
Description=Apache Tomcat Web Application Server
After=network.target
[Service]
Type=forking
User=tomcat
Group=tomcat
Environment="JAVA_HOME=/usr/lib/jvm/java-17-amazon-corretto" # Adjust if                                                                                         a different Java version is used
Environment="CATALINA_PID=/opt/tomcat/tomcat.pid"
Environment="CATALINA_HOME=/opt/tomcat"
ExecStart=/opt/tomcat/bin/startup.sh
ExecStop=/opt/tomcat/bin/shutdown.sh
ExecReload=/bin/kill -HUP $MAINPID
[Install]
WantedBy=multi-user.target
sudo systemctl daemon-reload
sudo systemctl enable tomcat
sudo systemctl start tomcat
sudo firewall-cmd --permanent --add-port=8080/tcp
sudo firewall-cmd --reload
sudo systemctl daemon-reload
sudo systemctl enable tomcat
sudo systemctl start tomcat
sudo nano /etc/systemd/system/tomcat.service
sudo systemctl enable tomcat
sudo systemctl start tomcat
